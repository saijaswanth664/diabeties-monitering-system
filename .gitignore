# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/

# Environment variables (contains secrets) - NEVER commit
.env
frontend/.env

# Database (SQLite local dev only - use cloud DB in production)
*.db

# Generated reports (runtime artifacts)
reports_generated/

# Node modules (auto-installed by npm install)
node_modules/
frontend/node_modules/

# Frontend build output
# Removed frontend/dist/ so it can be committed for Render deployment
# package-lock.json

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Firebase service account (sensitive)
firebase_service_account.json

# Temporary / scratch
scratch/
e -Force .git
